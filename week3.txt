week3 การinputค่า
#include <stdio.h>
#include <stdio.h>
char name[20]; //ประกาศchar20ตัว
int main()
{
    printf("Enter name:");
    gets(name);
    printf("name = %s\n", name); //ใช้ %s char
}
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
fgets(name,20,stdain);//กำหนดขนาดbuffer(เป็นเมมโมรีส่วนหนึ่ง)ได้
gets(name);การรับอินพุทเข้า
printf("n = %s , i = %d",n,i);//อินเดกสุดท้ายเก็บ\0 ทุกครั้งที่รับเข้ามาจะต้องเผื่อที่ให้\0 5ตัวที่เก็บถ้าพิม123 จะเก็บ123\n\0
getchar(c);//จะเอาตัวแรกที่พิมมามาใส่แม้จะรับมามาก
    puts(n);
    putchar(100);//100แอนกี้ การรับค่าเข้า
int found = 0;
    float isIn = 0.0;
    char isExist = '/0';//จะเป็นFทั้งหมด
    fgets(n,5, stdin);//การรับขนาด5ไว้ในnใช้ถึงแลป4
    int i = atoi(n);//ถ้าแปลงไม่ได้ใส่0

    if(i == 0){

    }//การเปรียบเทียบค่าสองค่าใน()
&& เป็นตัวเเละ และถ้าอยากเขียนว่าน้อยกว่าต้องใช้เเบบนี้ if(i > 0 && i < 20)
if(i > 0 && i < 20){
        printf("if\n");

    }else if (i < 100 ขีดบังสองตัว found && isExist){
        printf("else if\n");
    }else if (i>= 'A' && i<='z'){
        printf("else if 2\n");
    }else {
        printf("%d\n",i>0);
    }
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
fgets(n,5, stdin);
    int i = atoi(n);
    int count = 0;
    while(vount < 1){
        printf("%d\n", count);
        count++;
    }//การวนลูป ถ้าติดอินฟินิลูปใช้ctrl+C
    //++คือการบวกตัวแปรไป+1
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
fgetsรีเทรนค่ากลับเสมอ
for(int j = 0; j < i(ดูการเป็นจริงหรือเท็จถ้าจริงทำ); j++(ทำงานในลูปเสร็จแล้วทำหลัง)){
        printf("%d\n",j)
    }//วนiรอบ
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
for(;;){}//เป็นอินฟินิคลูป
    while(1){}//เป็นอินฟินิคลูป
