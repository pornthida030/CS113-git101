week5 การใช้pointer
#include <stdio.h>
#include <stdlib.h>
int main(){
    // int x =100;
    // printf("x = %d",x);
    // printf("reference of x = %lu\n",&x);
    // printf("reference of x = %p",&x);//%pปริ้นตัวเลขที่มีค่ามาก ๆ
    
                 //0 1 2 3  4
    int number[] ={7,8,9,10,11};//ต้องกำหนดขนาดให้อะเลย์ก่อนเสมอ
    //int number[100] ={7,8,9,10,11};
    number[1000] = 200;//ถ้ามีaddmentจะสามารถเปลี่ยนได้
    printf("index 3 =%d\n", number[1000]);//สามารถใส่อะเรย์ที่ไม่มีในอินเดกได้
    printf("reference index 3 =%p\n", &number[3]);//ดูว่าเป็นอินเดกที่เท่าไรใส่&
    printf("reference index 0 =%p\n", &number[0]);
    printf("reference index 0 =%p\n", number+3);//number+3บวกไป3เท่าของขนาดตัวแปร//poniter number 
}
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
int *ptr = &x;//pointerเก็บแอดเดด &เป็นตัวบอกแอดเดด
    int *arrPtr;//ตอนประกาศต้องมี*เสมอ
    arrPtr = number; //numberอย่างเดียวคือ &number[0]//ถ้าจะใส่ค่าให้มันจะต้องไม่มีดอกจันทร์
    arrPtr =arrPtr +1;//ชี้อินเดกหนึ่งบวกจะบวกไป4แอดเดด
    *arrPtr =*arrPtr +1;//*dereferencing เป็นการดึงค่าในนั้นมาโชว์
    printf("arrPtr = %p, *arrPtr = %d\n",arrPtr,*arrPtr);
    printf("number[1] = %p, number[1] =%d\n",&number[1],number[1]);
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
&x =50;//ไม่สามารถทำได้ไปเปลี่ยนเเอดเดด
ptr =x;//xต้องเป็นอะเรย์เท่านั้น
    ptr =50;//***ไปเปลี่ยนค่าแอดเดดโอเอสจะพัง**
int number[] ={7,8,9,10,11};//วนloop 5รอบ
    char intput[50];//วนตั้งแต่ตัวแรกไปจนถึง\0
    for(int *ptr = number;ptr<= &number[4];ptr++){
        printf("[%p] = %d\n",ptr,*ptr);
    }
|ผลลัพธ์ |
|[0028FF18] = 7|
|[0028FF1C] = 8|
|[0028FF20] = 9|
|[0028FF24] = 10|
|[0028FF28] = 11|
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
char input[50];
    int count =0;
    char c;
    while ((c = getchar())!= '\n'){
        input[count] =c;
        ++count;
    }
    input[count]='\0';//ถ้าไม่มีจะมีตัวแปลกๆขึ้นมาด้านหลัง
    printf("input = %s",input);//รับตัวแรกถึง\n
|ผลลัพธ์ |
|lol|
|input = lol|
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
input[count]='\0';//ตัวกั้นไว้ก่อนว่ามี\0
int number[] ={7,8,9,10,11};//วนloop 5รอบ
    char intput[50];//วนตั้งแต่ตัวแรกไปจนถึง\0
    for(int *ptr = number;ptr<= &number[4];ptr++){
        printf("[%p] = %d\n",ptr,*ptr);
    }
    char input[50];
    int count =0;
    char c;
    while ((c = getchar())!= '\n'){
        input[count] =c;
        ++count;
    }
    input[count]='\0';//ถ้าไม่มีจะมีตัวแปลกๆขึ้นมาด้านหลัง//ตัวกั้นไว้ก่อนว่ามี\0
    printf("input = %s\n",input);//รับตัวแรกถึง\n
    for (char *cPtr = &intput[0]; *cPtr != '\0'; cPtr++){
        printf("%c\n",*cPtr);
    }
int a;
    char c;
    char s[50];
    scanf("%c-%d-%s",&c,&a,s);//ต้องใส่&เพราะเป็นแอมเพอร์แซนธรรมดา
    printf("%c    %d    %s",c,a,s);//ไม่ต้องมี&เพราะprintf scanfต้องมี
|ผลลัพธ์ |
|t-12375-lol|
|t    12375    lol|