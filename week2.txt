Week2 ประกาศ Type ของตัวแปร และแสดงตัวแปรของแต่ละType
 
# include <stdio.h>
#define if 20;//เป็นการสร้างตัวแปรค่าคงที่ overline keyword ("%d", for) มีสีเป็นคำสั่ง 
int main (){
    int a = 10; //เป็นการอ้างตัวเเปรว่าเป็นอะไร จะต้องมีการเก็บค่าไว้1ค่า intเก็บค่าได้32บิต4ไบต์เก็บได้ประมาณ2**31(-2100000 ถึง 2100000)
    long long b = 400000000; //ถ้าเกินใช้longใหญ่กว่า 4 ไบต์ต้องประกาศเดต้าไทให้พอกัน
    unsigned int = 400000000; //เก็บเยอะกว่าlong
    unsigned long long b =400000000;//ใหญ่สุดจะปริ้นเป็น"%llu"

    float c = 10.22;//%f เริ่มต้นเป็น 0
    double d = 111.112;//จุดทศนิยม %lf

    char s = '\0'; //สติงว่างของ char
    char e = 'a';//type %c (''เป็นตัวกำหนดว่าเป็นchar) เก็บเป็นint '' ไม่มีว่างแบบนี้
    char name[] = "kong";//str type %s

    //printf("%ld", b);//longจะปริ้นด้วยตัวดังกล่าว บิตแรกเป็น1บิตอื่นเป็นศูนย์
    int f;
    printf("%d", f);//เป็นการจองพื้นที่
}//เป็นตัวเเปรโลคอล เพราะอยู่ในฟังก์ชัน ไม่ต้องกำหนดค่าเริ่มต้น
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
int ans1 = a + e *200;ทำการคูณได้charถ้าอยากทำซ้ำต้องใช้ลูป
float ans = 1/2;//อินหารอินได้อินเลยเป็น0.00000
float ans = 1.0/2;//จะได้เป็น0.5 จะต้องมีโฟด1ตัว
int ans = 13%15;//%ได้int ได้13 ถ้าตัวเเรกน้อยกว่าตัวหลังเอาตัวเเรกตอบได้เลย แต่ถ้าตัวแรกมากกว่าตัวหลังต้องมอด *ยกกำลังในCต้องวนลูปหรือใช้math **ถ้าเป็นแบบนี้ในCมันเป็นการเรียกซ้ำ
\n คือเป็นตัวที่Enterบรรทัด